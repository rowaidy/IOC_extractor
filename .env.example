# Copy to .env and fill in values before running docker compose up

# Required: Rails credentials master key
# Get the value from config/master.key (never commit this file)
RAILS_MASTER_KEY=your_master_key_here
